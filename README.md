A CLI utility for uncompressing files of rar, zip, etc. format. 
Currently only rar files are to be implemented. *unrar* should be
installed. 

The program does not handle errors of paths. 

### Usage 
Supply the paths as arguments.
If password is required, argument "-p <password>" should be supplied. 

